export type EnemyVariation = {
    name: string, 
    healthPoints: number,
    damage: number,
    moveSpeed: number,
}

export type Attributes = {
    healthPoints: number,
    damage: number,
    moveSpeed: number
}
