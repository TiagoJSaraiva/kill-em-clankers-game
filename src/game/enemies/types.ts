export type EnemyVariation = {
    name: string, 
    healthPoints: number,
    damage: number,
    moveSpeed: number,
    spriteName: string
}

export type Attributes = {
    healthPoints: number,
    damage: number,
    moveSpeed: number
}
